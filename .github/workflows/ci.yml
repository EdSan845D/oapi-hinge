name: CI

on:
  push:
    branches: [main, alpha]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: [contract, servergin, serverecho, openapi, example]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Build
        working-directory: ${{ matrix.module }}
        run: |
          if [ "${{ matrix.module }}" = "openapi" ]; then
            go build -tags openapi ./...
          else
            go build ./...
          fi

      - name: Vet
        working-directory: ${{ matrix.module }}
        run: |
          if [ "${{ matrix.module }}" = "openapi" ]; then
            go vet -tags openapi ./...
          else
            go vet ./...
          fi

      - name: Test
        working-directory: ${{ matrix.module }}
        run: |
          if [ "${{ matrix.module }}" = "openapi" ]; then
            go test -tags openapi ./...
          else
            go test ./...
          fi
